import React, { useState } from 'react';
import { X } from 'lucide-react';
export default function OnSiteEvidenceModal({ dispatch, onClose, onSubmit }) { const [outcome, setOutcome] = useState('Cash intercepted'); const [name, setName] = useState(''); const [notes, setNotes] = useState(''); if (!dispatch) return null; return <div className="fixed inset-0 z-50 bg-black/70 p-4 flex items-end sm:items-center"><form onSubmit={e => { e.preventDefault(); onSubmit({ outcome, suspect_details: name ? { name } : {}, evidence_urls: ['field-terminal://evidence/camera-capture'], gps_lat: dispatch.destination.lat, gps_lon: dispatch.destination.lon, notes }); }} className="w-full max-w-md mx-auto rounded-t-2xl sm:rounded-2xl bg-[#111827] border border-slate-700 p-5 space-y-4"><div className="flex justify-between"><div><h2 className="text-white font-semibold">On-Site Evidence Report</h2><p className="text-[10px] text-slate-500 font-mono">GPS + timestamp auto-tagged</p></div><button type="button" onClick={onClose}><X className="text-slate-400" /></button></div><select value={outcome} onChange={e => setOutcome(e.target.value)} className="w-full min-h-11 rounded-lg bg-[#0A0E1A] border border-slate-700 px-3 text-sm text-white"><option>Cash intercepted</option><option>Suspect detained</option><option>Empty ATM</option><option>False alarm</option></select><input value={name} onChange={e => setName(e.target.value)} placeholder="Suspect details (if detained)" className="w-full min-h-11 rounded-lg bg-[#0A0E1A] border border-slate-700 px-3 text-sm text-white" /><div className="rounded-lg border border-dashed border-slate-600 p-3 text-xs text-slate-400">Evidence capture armed: Photo • Video • Audio note</div><textarea value={notes} onChange={e => setNotes(e.target.value)} placeholder="Operational notes" className="w-full h-20 rounded-lg bg-[#0A0E1A] border border-slate-700 p-3 text-sm text-white" /><button className="w-full min-h-12 rounded-lg bg-cyan-600 text-white text-sm font-bold">Submit signed field report</button></form></div>; }
