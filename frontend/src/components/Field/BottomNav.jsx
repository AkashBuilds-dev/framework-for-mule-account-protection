import React from 'react';
import { Bell, MapPin, Camera, Clock } from 'lucide-react';
const tabs = [{ id: 'alerts', label: 'Alerts', icon: Bell }, { id: 'map', label: 'Map', icon: MapPin }, { id: 'actions', label: 'Actions', icon: Camera }, { id: 'history', label: 'History', icon: Clock }];
export default function BottomNav({ active, setActive, alertCount }) { return <><nav className="fixed bottom-0 inset-x-0 z-30 h-16 bg-[#0D1322]/95 backdrop-blur border-t border-slate-800 flex justify-around">{tabs.map(tab => { const Icon = tab.icon; return <button key={tab.id} onClick={() => setActive(tab.id)} className={`relative flex-1 min-h-11 flex flex-col items-center justify-center gap-0.5 text-[10px] ${active === tab.id ? 'text-cyan-300' : 'text-slate-500'}`}><Icon className="w-5 h-5" />{tab.label}{tab.id === 'alerts' && alertCount > 0 && <span className="absolute top-1/2 ml-6 -mt-4 bg-red-500 text-white rounded-full text-[8px] px-1">{alertCount}</span>}</button>; })}</nav><footer className="pb-20 pt-2 text-center text-[9px] font-mono text-slate-600">Ministry of Home Affairs | I4C Field Operations</footer></>; }
